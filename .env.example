# Azure Storage Configuration (optional - only needed for testing Azure Storage locally)
# Leave commented out for normal local development

# Force Azure Storage mode for local testing
# USE_AZURE_STORAGE=true

# Azure Storage Account (default: gtfspugetsound)
# AZURE_STORAGE_ACCOUNT=gtfspugetsound

# Azure Storage Container (default: puget-sound)
# AZURE_STORAGE_CONTAINER=puget-sound

# Azure Storage Connection String (optional - only needed for private containers)
# AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=...;AccountKey=...;EndpointSuffix=core.windows.net

# Server Port (default: 3000)
# PORT=3000
